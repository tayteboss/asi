import styled from 'styled-components';

const TokenSectionWrapper = styled.div``;

const TokenSection = () => {
	return <TokenSectionWrapper>TokenSection</TokenSectionWrapper>;
};

export default TokenSection;
