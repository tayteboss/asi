import styled from 'styled-components';

const VideoSectionWrapper = styled.div``;

const VideoSection = () => {
	return <VideoSectionWrapper>VideoSection</VideoSectionWrapper>;
};

export default VideoSection;
